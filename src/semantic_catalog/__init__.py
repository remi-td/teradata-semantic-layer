"""Teradata-native semantic catalog.

Public entry points:
    semantic_catalog.server.create_app    — FastAPI application factory
    semantic_catalog.db.get_pool           — connection pool accessor
    semantic_catalog.config.Settings       — parsed environment settings
"""

__version__ = "0.4.0"
